define(['backbone'], function(Backbone){
  var M_productItem = Backbone.Model.extend({
    defaults:function(){
      return {
        
      }
    },
    initialize: function() {

    },
    validate: function(attrs, options) {

    }
  })

  return M_productItem
});